# Create clone from repo
git clone <url>

# Add
git add <filename>

# Commit
git commit
git push

# Update
git pull

# Save Cretentials
git config --global credential.helper cache
git config credential.helper 'cache --timeout=1209600'
